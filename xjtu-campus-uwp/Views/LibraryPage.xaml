<Page
    x:Class="xjtu_campus_uwp.Views.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:xjtu_campus_uwp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:xjtu_campus_uwp.Models"
    mc:Ignorable="d">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Row="0">
            <ListView Name="BookGlanceListView" ItemsSource="{x:Bind BookList}" IsItemClickEnabled="True" ItemClick="BookGlanceItem_OnClick">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:BookGlance">
                        <RelativePanel>
                            <TextBlock Name="BookName" Text="{x:Bind Name}" />
                        </RelativePanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Row="1" Margin="0,10,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid Column="0">
                <TextBox Name="SearchTextBlock" FontSize="16"></TextBox>
            </Grid>
            <Grid Column="1">
                <Button Name="SearchButton" Content="Search" FontSize="16" Click="SearchButton_OnClick"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
